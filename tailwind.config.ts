import type { Config } from 'tailwindcss';

const config: Config = {
  darkMode: 'selector', // Use class-based dark mode (.dark class on html)
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
